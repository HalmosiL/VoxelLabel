# Template only -- copy to secret.yaml (gitignored) and fill in real values,
# or better, provision this Secret via an External Secrets Operator pulling
# from a cloud secret manager. Never commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: ct-platform-secrets
  namespace: ct-platform
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg://REPLACE_ME:REPLACE_ME@REPLACE_ME:5432/ctplatform"
  OBJECT_STORAGE_ACCESS_KEY: "REPLACE_ME"
  OBJECT_STORAGE_SECRET_KEY: "REPLACE_ME"

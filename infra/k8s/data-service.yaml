apiVersion: apps/v1
kind: Deployment
metadata:
  name: data-service
  namespace: ct-platform
spec:
  replicas: 3
  selector:
    matchLabels:
      app: data-service
  template:
    metadata:
      labels:
        app: data-service
    spec:
      containers:
        - name: data-service
          image: REPLACE_WITH_REGISTRY/data-service:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: ct-platform-config
            - secretRef:
                name: ct-platform-secrets
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
---
apiVersion: v1
kind: Service
metadata:
  name: data-service
  namespace: ct-platform
spec:
  selector:
    app: data-service
  ports:
    - port: 80
      targetPort: 8000

apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: ct-platform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: admin-service
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      containers:
        - name: admin-service
          image: REPLACE_WITH_REGISTRY/admin-service:latest
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: ct-platform-config
            - secretRef:
                name: ct-platform-secrets
          readinessProbe:
            httpGet: { path: /health, port: 8000 }
          livenessProbe:
            httpGet: { path: /health, port: 8000 }
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: ct-platform
spec:
  selector:
    app: admin-service
  ports:
    - port: 80
      targetPort: 8000
